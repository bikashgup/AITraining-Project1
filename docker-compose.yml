version: "2"

services:
    db:
        image: mongo
        container_name: "mongodb1"
        
        ports:
            - "27018:27017"
    edr_app:
        build: .
        command: flask run
        ports:
            - "5000:5000"
        depends_on: 
            - db

        network_mode: "host"